class Recipe():
    """ This class provide a way to store recipes in the database """

    def __init__(self, reciipe_name, recipe_picture, recipe_ingredients, recipe_instructions, recipe_portion, recipe_carbs_per_portion, recipe_calories_per_portion, recipe_fat_per_portion)
        name = self.recipe_name
        picture = self.recipe_picture
        ingredients = self.recipe_ingredients
        instructions = self.recipe_instructions
        portion = self.recipe_portion
        carbs = self.recipe_carbs_per_portion
        calories = self.recipe_calories_per_portion
        fat = self.fat_per_portion

